@model Estoque.Models.Usuario

@{
    Layout = "_Layout";
    ViewData["Title"] = "Acesso ao Sistema";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-dark text-white text-center py-4">
                    <h3 class="fw-bold mb-0">Login</h3>
                </div>
                <div class="card-body p-4">
                    @* Formulário de login *@
                    <form asp-action="Login" method="post">
                        @Html.AntiForgeryToken()
                        @* Resumo de validação *@
                        <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>

                        @* Campo para o email *@
                        <div class="mb-3">
                            <label asp-for="Email" class="form-label small fw-bold"></label>
                            <input asp-for="Email" class="form-control" placeholder="exemplo@email.com" />
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        @* Campo para a senha *@
                        <div class="mb-4">
                            <label asp-for="Senha" class="form-label small fw-bold"></label>
                            <input asp-for="Senha" type="password" class="form-control" />
                            <span asp-validation-for="Senha" class="text-danger small"></span>
                        </div>

                        @* Botão de envio *@
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg fw-bold">Entrar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>